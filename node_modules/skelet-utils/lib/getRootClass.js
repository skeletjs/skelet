function getRootClass (instance) {
  if (instance === null) return instance
  if (instance === undefined) return instance

  const result = Object.getPrototypeOf(instance)

  if (result === null || Object.getPrototypeOf(result) === null) return instance
  return getRootClass(result)
}

module.exports = getRootClass
