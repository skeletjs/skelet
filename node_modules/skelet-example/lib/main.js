const Module = require('skelet-core/lib/module')

class Skelet extends Module {
  async start () {
    console.log('start1')
    console.log('isloaded: ' + this.isLoaded)
  }
}

module.exports = new Skelet()
